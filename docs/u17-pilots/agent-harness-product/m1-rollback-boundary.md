# M1：应用后回滚边界记录

状态：当前首版边界已确认，不进入实现。

Atlas + OpenCode 的首版交付契约是“隔离执行、验证、差异审阅、用户显式应用、证据导出和清理”。应用完成后不自动生成或执行回滚/撤销操作；需要恢复时由用户使用项目自己的 Git 提交、`revert` 或备份策略处理。

这个边界与当前验收计划一致：首版不承诺自动回滚。失败、取消和超时发生在应用前时，Atlas 保留隔离副本与证据，源工作区不变；应用后的恢复属于后续独立产品能力，必须在确认恢复模型、权限硬门、备份生命周期和独立验收协议后再立项。

当前已通过的相关证据：

- `m1-mutation-acceptance.md`：变更执行、显式应用、独立验收和清理。
- `m1-stop-cleanup-acceptance.md`：停止、取消摘要、证据保留和清理。
- `m1-timeout-acceptance.md`：任务级超时、`timed_out` 证据和清理。
- `m1-failure-recovery-acceptance.md`：OpenCode 失败、诊断证据和清理。

因此，首版 Agent Harness 产品使用案例的 M1 核心闭环已完成；自动回滚不是本轮完成条件。
