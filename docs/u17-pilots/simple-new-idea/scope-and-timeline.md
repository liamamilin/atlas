# 范围与时间记录

状态：十三轮核心闭环已完成并通过产品验收；项目/任务删除最近删除与恢复已完成并同步 Atlas 代码基线。

## 固定边界

- 案例开始：2026-09-14T00:52:27Z。
- 第一轮：NI-R1、NI-R3——跨项目当天承诺与本地持久化。
- 第一轮范围外：时间块、冲突提示、外部日历、自动排程、账号、云同步和团队协作。
- 第二轮在第一轮普通生成和 Atlas 文档完成后披露：NI-R2、NI-R4——手工时间块与只读重叠提醒。
- 第二轮继续排除：自动排程或移动、外部日历、云同步、账号、完成状态、项目/任务删除。

## 时间线

| 事件 | UTC 时间 | 说明 |
|---|---|---|
| 协议冻结 | 2026-09-14T00:52:27Z | 模型、输入、来源指纹、范围和计时口径固定 |
| 普通生成开始 | 2026-09-14T01:00:32.378Z | 前两次 CLI 参数错误未调用模型；第三次使用相同固定输入 |
| 普通生成完成 | 2026-09-14T01:01:33.399Z | 总耗时 61.021 秒；模型响应阶段 52.218 秒 |
| 第一轮文档与需求确认 | 2026-09-14 | Atlas 保存分析及核心 4 份文档，确认 NI-R1/NI-R3 |
| 第一轮实现与验收 | 2026-09-14 | OpenCode 重试后应用；真实页面和进程重启验收通过 |
| 第二轮需求确认 | 2026-09-14 | 在首轮完成后确认 NI-R2/NI-R4，并记录 dec_0b095d… |
| 第二轮文档保存 | 2026-09-14 | 产品、技术、开发、验收文档保存为 v2 |
| 第二轮实现与验收 | 2026-09-14 | OpenCode 修改应用；固定命令 9/9 通过，真实页面验收通过 |

## 当前结论

- 前两轮目标在当时均已完成，Atlas 当时无活动迭代。
- 第二轮仍保留 6 个分析问题（时间块数量、移除语义、边界、时间表示、编辑范围、来源项目展示）；它们没有阻止已确认的最小实现，后续扩展前应先决策。
- 真实用户参与未观察；本案例由开发者代理执行，不能据此推断用户满意度。

## 第三轮：时间输入契约与数据完整性

第一、二轮已验证跨项目当天承诺、手工时间块、只读冲突提醒和重启持久化。第三轮只收紧服务层输入边界：时间必须是本地 `HH:MM`（00:00–23:59），结束时间必须晚于开始时间；非法请求返回明确的 HTTP 400，并且不得写入数据库。范围继续排除自动排程、外部日历、账号/云同步和删除级联设计。

- 迭代：`itr_b65caaa42ac740118e59cadfd4c650b6`
- 任务：`tsk_d9a38819f16d4e96bbcb68333f770cda`
- 执行：`exe_3a952e7bf30944a890cec951ff063fc2`
- 基线：`snap_f6635684824b4bf4b695e9507496e1b0`
- 验收：43/43 回归测试通过；真实 API 验证非法时间 400、合法时间 201，第三轮已完成。

首次测试尝试受到沙箱禁止临时 HTTP 端口绑定的环境限制，随后在允许本地端口的环境中复跑通过；该失败作为环境证据保留，不作为产品失败。

当前结论：三轮核心闭环均已完成。扩展多时间块、移除/删除级联、重叠边界和时间表示前，仍应先关闭 Atlas 中的开放分析问题。

## 第四轮：页面错误反馈与冲突提示稳定展示

第四轮在既有四条核心需求内做可用性收口，不扩展数据模型或自动排程：所有 API 操作失败时显示可读错误提示并保留当前页面数据；冲突提醒写入独立区域，避免重复渲染时闪烁或被列表内容覆盖；成功操作继续刷新列表。

- 迭代：`itr_fac58c9b9791487eb5587c11a09e04ea`
- 主执行：`exe_e8b798c1704444a69712c18f77197b45`，应用并通过验收。
- 冲突提示修正重试：`exe_ad6ea0ce2f184e549f8ae08a20bbd4e0`，应用后对重复任务明确免验。
- 改动：`static/app.js`、`static/index.html`、`static/styles.css`，并增加错误路径回归测试。
- 验收：54/54 测试通过；停止服务后页面显示 `加载当天承诺失败: Failed to fetch`；恢复服务后重叠时间块显示 1 条冲突和两个 `Overlap` 标记。

当前结论：四轮核心闭环已完成。接下来应先处理时间块关系、删除/移除级联、重叠边界和编辑范围等开放决策，再进入项目/任务删除或更大范围扩展。

## 第五轮：项目与任务生命周期

第五轮在四轮已验收能力上补齐工作台日常维护：项目名称和任务标题可编辑并持久化；项目和任务删除必须经过明确的 Confirm/Cancel 二次确认。删除项目级联清理其任务、当天承诺和时间块，删除任务级联清理其承诺和时间块；删除不会影响其他项目，移除当天承诺仍只解除关系并保留源任务。撤销、云同步、自动排程和完成状态继续排除。

- 需求：`req_2f7f4298f21e44968d22c4e4866212de`
- 迭代：`itr_a6fea3387b5944d8ac0e02cc5ebd3f78`
- 任务：`tsk_dcf4f91a5ec04148b0b25297cbf436d6`
- 执行：`exe_1f6b30e226864348b9c2a5d9ab6f2e58`
- 功能验收基线：`snap_7bbf94af74674512a06b59b6d2bc8b5a`；文档提交后同步基线：`snap_46b6a5392ee840d2af7251dcb7b62c19`
- 验收：80/80 workspace tests、`node --check static/app.js` 和真实页面编辑/取消删除/确认删除均通过。

独立复核发现代理结果中的 `renderProjects()` 模板字符串缺少结束反引号，导致页面脚本无法解析；已在应用后修复并重新进行语法、测试和页面验收。验收测试产生的 `planner.db` 已可逆移出工作区，未删除未知用户数据。

## 第六轮：任务完成状态

第六轮补齐安排到执行的反馈闭环：任务可由用户显式标记为完成或恢复未完成，状态持久化并在项目任务列表与当天承诺清单显示。完成或恢复不会自动移除承诺、删除任务、删除或移动时间块；自动归档、撤销、云同步和自动排程继续排除。

- 需求：`req_d0b828bdb2514772846ffeccac3435c1`
- 迭代：`itr_72ef18b19de7449685b16486f5361d06`
- 任务：`tsk_a86b7a9ec8eb4f1da54be6ec6347e204`
- 执行：`exe_b692ead5c9f242e38b55e8f8b6f4aeb4`
- 基线：`snap_df59bde63f29454885c562fd28a5c57f`
- 验收：103/103 workspace tests、`node --check static/app.js` 和真实页面完成/恢复切换均通过；恢复任务后当天承诺仍保留。

## 第七轮：本地数据 JSON 备份导出

第七轮在六轮已验收的项目、任务、承诺、时间块、冲突提示、编辑删除和完成状态基础上，补齐本地优先数据的主动备份出口。用户点击页面入口后下载单个 JSON 文件，文件包含 `schema_version`、`exported_at`、项目、任务、当天承诺、时间块和任务 `completed` 状态。导出只读当前 SQLite 数据，不写入或改写任何源记录；本轮不做导入覆盖、自动备份或云同步。

- 需求：`req_93d609a3079a493ea8196aa5620c4769`
- 迭代：`itr_219bfad06719402a9f316e8f4fec74a7`
- 任务：`tsk_ff1250adc2484974bef41c899fdbeffc`
- 执行：`exe_dac822354f244c0786132cb74f9125ac`
- 基线：`snap_959f76e7099044d2a154492ba1e6bf58`
- 验收：117/117 workspace tests、`node --check static/app.js` 通过；真实服务返回 200 JSON，Chrome 实际下载 `planner-backup.json`，真实页面显示导出入口；导出前后项目 API 数据一致。

第七轮 OpenCode 完成报告缺失且 requirement IDs 不完整，Atlas 未把 agent 自报当作完成事实；独立测试、接口结构、只读比对和页面入口证据均已补足，产品验收通过。

## 第七轮后的边界决策固化

为避免下一轮恢复功能返工，Atlas 追加两条决定：一条承诺允许多个时间块，移除承诺级联清理时间块，冲突只比较同日且首尾相接不算重叠；另一条固定备份 `schema_version=1.0`，导入/恢复必须独立确认，先校验和预览，不允许静默覆盖。基于这两条决定重新生成并保存产品、技术、开发和验收文档 v6；未新增代码范围，轮次计数仍为七轮。

- 时间块边界决定：`dec_7d04691fc1bc4c5389d580ad58d69659`
- 备份恢复边界决定：`dec_0872de402eec48b8a75cae9763a6ba92`
- 文档版本：产品 `dver_b159c1cb4b19454eae59a49ab12571f2`；技术 `dver_3e3770b71fdd441fb3efb3daa9e17d6c`；开发 `dver_c2ed5370d4a34d2da56d0435cdd99308`；验收 `dver_ea4be5da72414d578173555028cef3ad`。


## 第八轮：本地备份导入预览与恢复

第七轮已提供 `schema_version=1.0` 的 JSON 导出，第八轮补齐恢复闭环。用户选择备份文件后，系统先校验版本、结构和四类记录的引用完整性，并展示项目、任务、当天承诺和时间块数量；只有确认对话框明确确认后，才在事务中完整替换本地数据。校验失败、取消或恢复异常都不应留下部分写入；本轮不做合并导入、云同步或静默覆盖。

- 需求：`req_014a9d585ccc482580389031c4e93545`
- 迭代：`itr_8755e0e9381c497eadfcd729cd707b34`
- 任务：`tsk_d40dab4317e84b579cad686c6adc6a39`
- 执行：`exe_e731755dd05e4aabb16ffd9276b33637`
- 文档：产品 `dver_a4dc6fa86769422d927b7d1bd04b2a93`；技术 `dver_2171109624e94aa79bbd5f2a2a48ee8a`；开发 `dver_19c693b944454a18822ca7241ed69be3`；验收 `dver_a396277c69e24a478bf2402a48bb7cf3`。
- 基线：执行前 `snap_29475dc62ef441c782220f63d642c1f9`；结果 `snap_213f289e7571455fa83b54a838c5c763`；应用后 `snap_a368c546998c4be3bf0a9c1353ea309b`。
- 验收：149/149 workspace tests、`node --check static/app.js` 通过；真实服务和页面验证有效预览、错误版本、确认取消与明确确认恢复。

当前结论：第八轮核心功能已完成。下一步应在提交后同步最终工作区基线，再评估备份文件管理、导入前自动快照或更完整的文档归档，而不是扩大当前恢复语义。

## 第九轮：备份恢复 API 安全加固

第九轮没有改变“预览后确认、完整替换、失败回滚”的恢复语义，而是把不可静默覆盖和数据完整性边界下沉到服务层。`POST /api/import` 必须收到 `confirm=true` 才会执行写入；导入前会拒绝重复项目、任务、当天承诺、时间块 ID，拒绝非布尔 `completed`，拒绝无效真实日期，拒绝非法 `HH:MM` 时间和结束时间不晚于开始。

- 需求：`req_24667b37cb2e4d6085efd614cb4df918`
- 迭代：`itr_3ec5a7963d0248acacdcc6fcc52a9daf`
- 任务：`tsk_71fcbd88003e498c80bc19c56707ab48`
- 执行：`exe_cd5bffd24ec04900bdd9c78753b8ae8b`
- 文档：产品 `dver_90df58b1b62e41b492c97b20009e4fc8`；技术 `dver_5e445b54bbec4660847024786728511b`；开发 `dver_aeca773bcaa84887938ac6ec6184b232`；验收 `dver_9f7aab95ec3b45a493e240105f513d3d`。
- 基线：执行前 `snap_9fe3a1187bd04813b925bedcbcfefe62`；独立修复和临时运行库移出后最终代码基线 `snap_00660443ec2f4fb4a87d324a0d9ab054`。
- 验收：177/177 workspace tests、`node --check static/app.js` 通过；真实 API 验证未确认、重复 ID、坏类型、坏日期、坏时间和结束顺序错误均返回 400 且不写库，确认恢复成功并保留 `completed`。

独立复核发现两个边界缺口并已修复：前端确认按钮需要把 `confirm: true` 传给服务端；日期不能只匹配 `YYYY-MM-DD` 形状，还必须是可解析的真实日期，例如 `2026-99-99` 应拒绝。

## 第十轮：导入恢复前自动安全快照

第十轮围绕第八、九轮已确认的备份恢复边界继续收口，没有新增合并导入、云同步或自动排程。确认恢复仍是完整替换；区别是服务端在实际覆盖本地数据库前，会先把当前数据库导出为一个本地 JSON 安全快照。只有通过 `confirm=true` 和全部导入校验后才创建快照；预览、缺少确认、结构错误和字段校验失败均不会写入快照目录或数据库。若安全快照写入失败，恢复会中止并返回错误，原数据库保持不变。

- 需求来源：围绕 `req_014a9d585ccc482580389031c4e93545` 和 `req_24667b37cb2e4d6085efd614cb4df918` 的恢复安全增强；本轮未新增 Atlas 候选需求 ID。
- Atlas 基线：第九轮文档提交后基线 `snap_c85e368eac3841bca2192f6bcae93480`；第十轮代码安全快照实现后基线 `snap_087b8a93d7c44310beada6857533ea16`。
- 改动：`app.py` 新增按数据库文件名隔离的 `backups/<db-name>/pre-import-*.json` 安全快照；`POST /api/import` 在确认和校验通过后、替换前保存当前数据；`static/app.js` 在恢复成功提示中显示安全快照路径；`tests/test_app.py` 增加快照创建、失败不创建和快照写入失败中止恢复的回归测试。
- 验收：178/178 workspace tests、`node --check static/app.js` 通过；真实 API 验证预览、缺少确认和无效导入都不创建安全快照，确认有效恢复会创建包含覆盖前项目与任务的 JSON 快照，并完成恢复。

第十轮验收产生的 `planner.db`、`backups/` 和 `__pycache__` 已可逆移出工作区至 `/private/tmp/atlas-round10-runtime-20260915/`。

## 第十一轮：备份 schema 迁移入口

第十一轮不改变当前对外备份格式，导出仍固定为 `schema_version=1.0`，导入仍只接受受支持版本。变化是把版本识别、未来迁移和当前结构校验拆成明确管线：导入预览和确认恢复都先进入 `prepare_import_backup()`，由它调用 `migrate_backup_to_current()` 得到标准化备份，再用当前 schema 校验函数检查字段、引用和时间边界。这样以后增加 `schema_version=1.1` 时，可以新增迁移分支，而不用重写恢复事务和已有校验。

- 需求来源：围绕 `req_014a9d585ccc482580389031c4e93545` 和 `req_24667b37cb2e4d6085efd614cb4df918` 的版本兼容增强；本轮未新增 Atlas 候选需求 ID。
- Atlas 基线：上一份已接受基线为第十轮提交后同步 Git HEAD 的 `snap_be884d28192b4f00a3f9b30334bdcdba`；第十一轮代码变化已采用新基线 `snap_325462ab56c64d3291439aa2084596ec`。
- 改动：`app.py` 新增 `BACKUP_SCHEMA_VERSION`、`SUPPORTED_IMPORT_SCHEMA_VERSIONS`、`migrate_backup_to_current()`、`validate_current_import_backup()` 和 `prepare_import_backup()`；恢复写库改用标准化后的备份对象。`static/app.js` 在未来实际发生迁移时显示源版本到目标版本的提示。`tests/test_app.py` 增加当前版本深拷贝迁移、支持版本列表、预览标准化和缺失/未知版本拒绝测试。
- 验收：`node --check static/app.js` 通过；`TestImportValidation` 28/28、`TestImportBackupDataLayer` 6/6、全量 182/182 workspace tests 通过；临时 SQLite 数据库直接验收通过，覆盖导出 1.0、预览 source/current 版本、未知 2.0 拒绝、安全快照和恢复后 completed 状态。

真实 HTTP 临时端口验收已补跑通过：导出、预览、未知版本拒绝、确认恢复前安全快照和恢复后列表状态均通过；同时保留不绑定端口的临时数据库直接验收作为补充证据。

第十一轮产生的空运行库、缓存、临时直接验收和临时 HTTP 验收产物已可逆移出工作区至 `/private/tmp/atlas-round11-runtime-20260915/`、`/private/tmp/atlas-round11-direct-acceptance-20260915/` 与 `/private/tmp/atlas-round11-http-acceptance-20260915/`。


## 第十二轮：当天承诺按来源项目分组

第十二轮不改变数据模型和 API 契约，沿用承诺接口中已有的 `project_id` 与 `project_name`。变化集中在页面呈现：当天承诺清单按来源项目分组，每个项目块显示项目名和任务数量；可加入任务列表也按项目分组，只列出当前日期尚未加入承诺的任务。这样用户从“选择任务”到“查看当天安排”都能看到清晰的项目上下文。

- 需求来源：围绕 `req_e3e86690c6b243c9b66a0212050a1e64` 的跨项目当天承诺主流程，以及此前分析建议“在当天承诺清单中显示来源项目并按项目分组”。
- Atlas 基线：上一份已接受基线为文档提交后同步 Git HEAD 的 `snap_0924043d4e8b42d2b38b471d7cb9c3c3`；第十二轮代码变化已采用新基线 `snap_e238025089d34efaa9739d984910d0c0`。
- 改动：`static/app.js` 新增项目分组与组标题渲染函数，并把承诺项渲染拆成独立函数；`static/styles.css` 增加项目组、组标题、数量胶囊和可加入任务项样式；`tests/test_app.py` 补充 `project_name` 回归断言。
- 验收：`node --check static/app.js` 通过；Node 前端渲染验收通过；`TestCrossProjectCommitments` 5/5、全量 182/182 workspace tests 通过；临时产品实例浏览器验收确认 Alpha/Beta 项目分组和可加入任务分组正确。

第十二轮临时浏览器验收数据库已可逆移出工作区至 `/private/tmp/atlas-round12-ui-acceptance-20260915/`。

## 第十三轮：项目/任务最近删除与恢复

第十三轮补齐第五轮删除功能的安全余量：项目或任务仍需要明确确认才会删除，但删除前会保存本地快照，并在工作台显示 Recently Deleted。用户可以恢复项目或任务；恢复会带回相关任务、当天承诺和时间块，并在写入前检查 ID 冲突。任务级恢复要求源项目仍存在；导入备份会清空旧的最近删除记录，以避免旧撤销历史跨越新的备份状态。导出备份格式仍保持 `schema_version=1.0`，不包含最近删除历史。

- 需求来源：围绕第五轮项目/任务删除的误删风险收口，关联 `req_2f7f4298f21e44968d22c4e4866212de`。
- Atlas 状态：上一份已接受基线为 `snap_d3deba5d80d24900ae51dc75372a1422`；第十三轮代码变化已采用新基线 `snap_79397bd065374386b8020432678c3597`。
- 改动：`app.py` 新增 `delete_events`、删除前快照、最近删除列表和恢复 API；`static/index.html`、`static/app.js`、`static/styles.css` 新增 Recently Deleted 区域和恢复按钮；`tests/test_app.py` 增加恢复、冲突、导入清理和 API 回归测试。
- 验收：`python3 -m py_compile app.py tests/test_app.py`、`node --check static/app.js` 通过；`TestDeletedItemRestore` 5/5、`TestEditDeleteAPI` 17/17、全量 192/192 workspace tests 通过；临时产品实例浏览器验收确认最近删除可见、恢复后项目/任务/当天承诺/时间块回到页面。

第十三轮临时浏览器验收数据库位于 `/private/tmp/atlas-round13-ui-acceptance-20260916/`，未写入项目工作区。

## 下一步计划

核心功能当前已经完整到“本地项目计划器可日常使用，并具备手动导出、校验恢复、恢复确认、安全快照、失败回滚、schema 迁移入口、跨项目分组扫描和删除后恢复”的程度。后续建议按必要性排序：

1. 处理导出与导入期间的并发写入策略；本版仍按单用户本地使用假设。
2. 增加备份文件管理入口，例如查看最近安全快照、复制路径或打开所在目录。
