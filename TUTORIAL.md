# Application Atlas 使用教程

> 这份教程教你：查软件类型、看真实产品、把类型知识变成一个有资料、有需求、有文档、有执行、有验收的开发项目。

**三种读法**

- 想先跑起来：读「0. 五分钟跑通最小路径」，照着做一遍。
- 想完整掌握：按顺序读「1 → 4 → 5～8」，再挑「9. 两条完整路线」里和你接近的那条，最后看「9.3 最佳实践案例」。
- 卡住了：直接查「12. 排障」，或看项目页顶部「当前状态」卡片里的「下一项」。

---

## 0. 五分钟跑通最小路径

前提：服务已打开（见「2. 打开与关闭」）。假设你有一个新想法。

| # | 位置 | 动作 | 完成标志 |
|---|------|------|----------|
| 1 | 首页搜索框 | 输入想法关键词，开始搜索 | 出现「软件类型 / 真实应用」两类结果 |
| 2 | 类型结果页 | 点一个最接近的类型，阅读「定义核心」 | 你能一句话说出这类软件管什么 |
| 3 | 类型页 | 点 **「基于此类型创建项目」**，填名称和目标 | 跳进项目工作区，项目里已有 1 条固定资料 |
| 4 | 项目 · 阶段一 | 在工作区基线面板点 **「建立项目基线」** | 出现「已接受基线」 |
| 5 | 项目 · 阶段一 | 点 **「分析固定资料」**，等运行完成 | 出现候选需求、开放问题、建议 |
| 6 | 项目 · 阶段二 | 在「待处理事项」里回答关键问题；把候选需求点 **「加入候选」** | 计数下降、需求进入「范围与验收」 |
| 7 | 项目 · 阶段二 | 展开需求卡片，选 **本版做** 并 **「保存用户确认」** | 需求出现「用户已确认」 |
| 8 | 项目 · 阶段三 | 选「核心 4 份」→ **「生成 N 份草案」**，按顺序逐份保存 | 出现 4 份关联文档 |
| 9 | 项目 · 阶段三 | 每份文档点 **「批准当前版本」** | 文档出现「已批准」 |
| 10 | 项目 · 阶段三 | 填迭代标题和目标，点 **「创建并启动迭代」** | 出现「活动迭代」 |
| 11 | 项目 · 阶段四 | 添加任务（写清目标、写入路径、验证命令），点 **「保存任务」** | 任务卡出现 |
| 12 | 项目 · 阶段四 | 点 **「启动 OpenCode」** | 任务卡显示执行状态，实时刷新 |
| 13 | 项目 · 阶段四 | 执行完成后核对证据 → **「应用并更新项目基线」** | 结果状态变为 `applied` |
| 14 | 项目 · 阶段四 | 填验收证据 → **「验收通过」** → **「完成迭代」** | 迭代状态变为 `completed` |

每一步的细节和「为什么」在后面的对应章节里。

---

## 1. 心智模型：三样东西、一条流水线

### 1.1 Atlas 里只有三类东西

| 类别 | 是什么 | 谁负责 | 例子 |
|------|--------|--------|------|
| **类型知识** | 1805 种软件类型的正式资料，只读 | Atlas 语料 | 「AI Coding Assistant 的四条定义核心」 |
| **项目知识** | 你的项目里被固定的资料、需求、决定、文档版本 | 你确认，Atlas 保存 | 「TUI 是本版主要交互界面」 |
| **执行与证据** | OpenCode 在隔离副本里的改动，以及文件、命令、报告 | 引擎产出，你核对 | 「改了 src/app.py，测试通过」 |

一句话：**类型知识给依据，项目知识下决定，执行与证据只证明发生了什么。**

### 1.2 一条流水线

```
资料 ──→ 开放问题 / 候选需求 / 决定 ──→ 文档 ──→ 迭代输入 ──→ 执行 ──→ 应用 ──→ 验收
        （确定范围）                    （冻结计划）            （执行与验收）
```

对应项目页的四个阶段：**理解项目 → 确定范围 → 冻结计划 → 执行与验收**。

每一步都有前置条件，跳步会被按钮锁住或报错；这页底部「项目路径」的九个小标签会标出你现在在哪一步。

### 1.3 必须分清的三件事

这三件事看起来像，实际完全不同——教程后面反复用到：

| | 谁说了算 | 在哪里看 |
|---|---|---|
| **AI 建议** | 模型，仅供参考 | 分析运行结果、候选需求、参考建议 |
| **用户确认** | 你，必须显式保存 | 「用户已确认」、保留的文档、通过的验收 |
| **系统事实** | Atlas 校验过的数据 | 固定版本、文件指纹、执行状态、失败原因 |

**AI 永远不会替你确认**：它给建议范围，你保存的才是确认；它写文档草案，你批准的才算数；它报完成，你的验收才是结论。

---

## 2. 打开与关闭

- 打开：双击 `apps/Atlas.app`（等价于执行 `zsh apps/atlas-launch.sh`）。它会启动本地服务并打开 http://localhost:5188 。
- 关闭：终端执行 `zsh apps/atlas-stop.sh`。放着不管，超过 30 分钟无活动会自动关闭。
- 浏览器关掉不等于服务停止；重新双击图标是幂等的，只会再打开页面。
- 页面是本地实时服务：代码或文档更新后，刷新浏览器即可看到，不需要重新安装 App。
- 数据都在你自己的机器上：项目存在本地项目库，源码在工作区目录。

---

## 3. 找资料：搜索、类型页与应用页

### 3.1 首页 `/`

- 搜索框：输入关键词回车，跳到搜索结果页。
- 三个统计卡：类型叶子 / 导航节点 / 真实产品，点进去分别浏览。
- 按领域浏览：29 个顶级域的卡片，点击进入类型树对应位置。

### 3.2 搜索 `/search`

一次搜索同时覆盖**软件类型**和**真实应用**：

- 类型：匹配中英文名、别名和定义核心。
- 应用：匹配产品名、厂商、简介和任务标签。

没有结果时，换个更通用的词，或直接去类型树按领域找——这不会阻塞你已有项目的进度。

### 3.3 类型页 `/types/:slug`

这是知识库的核心页面。写项目时你主要用两处：

- **「基于这个类型开发项目」**：填写项目名后创建，并把**完整类型正文固定为项目第一条资料**。这是最省事的起点。
- 底部**正式资料阅读器**：切换「类型正文 / 研究笔记」，选「完整文档」或某一章节，填「借鉴点 / 项目备注」，点 **「收藏当前范围」** 把这一版固定进当前项目。

如果已经有当前项目，类型页顶部按钮是 **「加入当前项目并开发」**，直接把这版资料加进去。

### 3.4 应用实证 `/apps`

33 个真实产品实例。卡片上有资料深度说明和 Atlas 归类；点 **「收藏目录资料」** 把这条件记录加进当前项目；点归类类型可跳到类型页。

### 3.5 资料固定的规则

- **收藏时固定当时版本**（服务端重新读取并保存来源指纹）。
- 之后语料更新只会提示「正式资料已有新版本」，**不会改写旧记录**。
- 要跟进新版，回类型页重新收藏一次，旧记录保留。
- 在「项目」列表页可以管理已有资料：阅读状态（未读 / 已读 / 已复核）、借鉴点备注（点 **「保存备注」**）、**「查看固定摘录」**、**「刷新版本状态」**。

---

## 4. 项目工作台地图

### 4.1 顶部「当前状态」卡片

打开项目详情页，先看这张卡：

- **活动迭代**：有就显示编号和标题，没有就明确写「尚未建立」。
- **下一项**：系统推荐你接下来做的事，点它直接跳到对应阶段。
- **六类计数**：开放问题 / 明确冲突 / 参考建议 / 待确认需求 / 待复核文档 / 未批准文档。有数字是黄色，清零才算这一块收干净。
- **阶段入口**：四个阶段都可点击跳转。

### 4.2 四个阶段

| 阶段 | 做什么 | 包含面板 |
|------|--------|----------|
| **一 · 理解项目** | 固定资料、看工作区现状、运行分析 | 项目路径、工作区基线、资料分析与文档草案 |
| **二 · 确定范围** | 处理开放项、确认需求、记录决定 | 待处理事项、一致性信号、范围与验收、已记录决定 |
| **三 · 冻结计划** | 生成并批准文档、建立迭代 | 关联文档与版本、建立活动迭代 |
| **四 · 执行与验收** | 任务执行、应用、验收 | 迭代与开发执行 |

### 4.3 状态词表（卡住时查这里）

| 对象 | 状态 | 含义 |
|------|------|------|
| 需求范围 | 待决定 / 本版做 / 以后做 / 不做 | AI 建议不等于确认；只有你保存过才是「已确认」 |
| 需求修订 | `r1`、`r2`… | 改内容/验收/来源会生成新修订；迭代固定的是创建时的修订 |
| 文档新鲜度 | 依据为当前 / 需要复核 | 上游需求、文档或基线变化后会「需要复核」 |
| 文档审阅 | 待人工审阅 / 已批准 / 已拒绝 | 只有**已批准且依据为当前**的文档能进迭代 |
| 生成运行 | 正常 / 固定输入已过期 | 生成后资料/需求/决定有变化；应用前需确认 |
| 开放项 | 待处理 / 已回答 / 已解决 / 已采纳 / 已暂缓 / 已忽略 | 处理结果持久保存 |
| 执行 | 待执行 / 排队中 / 执行中 / 等待权限 / 等待输入 / 执行完成 / 执行失败 / 已停止 / 待核对 | 「执行完成」才谈得上验收 |
| 应用 | `pending` / `applying` / `applied` / `conflict` / `failed` / `superseded` | `applied` = 文件已写回且新基线已采用 |
| 验收 | 待验收 / 通过 / 失败 / 免验 | 通过和免验必须填理由；失败不能通过 |
| 迭代 | 活动 / 已完成 / 已放弃 | 结束后历史只读 |

---

## 5. 阶段一 · 理解项目

**为什么**：所有分析和文档只能引用**项目里已固定的资料**，所以资料和现状必须先到位。

### 操作

1. **补齐资料**（见第 3 节）。新想法通常 1～3 条够用：一条类型正文 + 需要的应用记录。
2. **建立或更新工作区基线**：
   - 第一次：点 **「建立项目基线」**。Atlas 会扫描工作区全部文件的指纹，并读取入口文档、工程清单和重点路径的正文。
   - 之后：点 **「检查外部变化」** 只做比对，不会改动已接受基线。检测到变化时会出现 **「采用当前状态为新基线」**。
   - 「重点读取路径」（每行一个相对路径）留空时仍会读 README、docs 和工程清单。
3. **运行分析**（三选一或都用）：
   - **「分析固定资料」**：产出开放问题、明确冲突、参考建议、AI 候选需求。
   - **「评估想法价值（仅建议）」**：只给观察、未知、替代方案、风险与验证实验；不评分、不改变范围、不阻止后续工作。
   - **「生成项目改进方案」**（已有项目）：结合基线和资料，产出当前/期望行为、影响范围、任务依赖与回归条件。

### 检查点

- 基线卡片显示「已接受基线」，并有文件数、Git 状态和指纹。
- 分析运行状态是 `completed`，面板里能看到候选需求。
- 选项：展开 **「查看现状证据报告」** 确认基层事实。

### 易错点

- **工作区有未采用的变化时，改进方案会拒绝启动**——先检查并采用，或先说明这些变化。
- **分析只读固定资料**：你后加的资料不会追溯进旧运行；需要时重新运行分析。

---

## 6. 阶段二 · 确定范围

**为什么**：范围是你和模型之间的分界。Atlas 把「AI 建议」和「用户确认」分开保存，永远不替你拍板。

### 6.1 处理开放项（「待处理事项」）

这里汇总每个模式**最近一次完成运行**的问题、冲突和建议：

| 类型 | 可以做什么 |
|------|------------|
| 开放问题 | **「保存回答并记录决定」**（回答会存成决定内容）；**「暂缓」**；**「忽略」** |
| 明确冲突 | **「标记已解决」**；**「转为决定」**；**「说明不冲突」** |
| 参考建议 | **「转为候选需求」**；**「转为决定」**；**「暂缓」**；**「忽略」** |

处理过的条目收进「已处理 N 项」，可以回看答案、状态和转出的决定 / 需求编号。价值分析产生的事项标「仅供参考」，处理结果同样保存。

### 6.2 确认需求（「范围与验收」）

- 分析的候选需求要逐条点 **「加入候选」** 才会进入项目。
- 也可以手动 **「添加候选需求」**：需求内容、分析建议、建议理由、验收条件（每行一条）、关联固定资料。
- 展开需求卡片里的 **「确认范围或编辑需求」**：
  - **用户确认**：选 **本版做 / 以后做 / 不做**，填确认理由，点 **「保存用户确认」**。
  - **修改需求内容**：改内容、验收条件、来源，点 **「保存需求修订」**。有意义的修改会**清除旧确认**并把关联文档标为「需要复核」，同时生成新的**修订号**（`r1` → `r2`）。
- 已建立的迭代固定的是**创建当时的修订**，后来的修改不会改变它；要换新修订，建新迭代。

### 6.3 看「一致性信号」

四个指标：**明确冲突 / 需要决定 / 参考建议 / 关联复核**。面板会写明统计来源（最近一次完成的固定输入分析及其运行 ID）；价值分析和你在生成历史里选中的其它运行不计入。信号只统计**未处理**的项，所以处理动作会让数字下降。

### 检查点

- 至少 1 条需求显示「用户已确认」且范围是 **本版做**。
- 待处理计数按你的处理下降；「明确冲突」不再是「未检查」。
- 关键取舍在「已记录决定」里有记录。

### 易错点

- **按钮一直灰**：确认需求时「确认理由」是必填。
- **改了需求，文档变「需要复核」**：这是设计行为，不是错误——重新批准或保存新版本即可。

---

## 7. 阶段三 · 冻结计划

**为什么**：执行必须绑定一份不会中途变化的输入——确定的文档版本、需求修订和工作区基线。

### 7.1 生成文档

在「资料分析与文档草案」里选：

- **核心 4 份**：产品需求、技术方案、开发规划、验收方案。
- **完整 6 份**：再加调研与分析、交互与页面。
- 也可自由勾选。生成前需要**至少 1 条已确认本版需求**。

草案必须**按依赖顺序逐份保存**：依赖没保存时按钮是灰的并提示「请先保存 xxx」。保存动作是 **「创建关联文档」**（首次）或 **「保存为新版本」**。

### 7.2 审阅并批准（「关联文档与版本」）

每份文档可以：

- 在**人工审阅**行填审阅说明，点 **「批准当前版本」** 或 **「拒绝当前版本」**。**只有已批准且依据为当前的版本能进迭代。**
- 展开「查看正文、编辑和版本」直接改 Markdown 并保存新版本（每次保存都是不可变版本）；新版本会把人批准重置为「待审阅」。
- 看「版本历史」、点 **「比较最近两版」**。
- 用 **「用自然语言修改一个章节」**：选章节 → 填要求 → **「生成修改候选」** → 看差异 → **「保存为新版本」**。AI 只能改所选章节，标题和其它内容必须逐字保持。

如果生成之后你改了资料、需求或决定，这个运行会标 **「固定输入已过期」**：结果仍可查看，但保存/加入按钮锁住，需要先点 **「确认仍要应用过期结果」**。

### 7.3 建立迭代

点 **「创建并启动迭代」**，需要：

- 迭代标题 + 本轮目标；
- 勾选**已批准且依据为当前**的文档版本；
- 勾选**已确认本版**的需求。

有缺口时按钮上方会列出缺少什么。创建时同时**固定需求修订和当前已接受基线**，滚动区可以回看「固定文档版本 / 本版需求（固定修订）/ 固定工作区基线」。

### 检查点

- 文档显示「已批准」，迭代表单能勾选到它。
- 「当前状态」卡片的「未批准文档」「待确认需求」计数清零。
- 活动迭代出现，阶段三里显示冻结摘要。

### 易错点

- **提示「approve every fixed document version」**：有文档没批准。
- **提示 rebase**：某个任务应用结果后基线前进了，按第 8.4 节重新对齐。

---

## 8. 阶段四 · 执行与验收

**为什么**：引擎执行 ≠ 任务完成 ≠ 产品验收。Atlas 把这三件事分开记录，防止自报成功混进结论。

### 8.1 添加任务

在「迭代与开发执行」右侧填：

| 字段 | 说明 |
|------|------|
| 任务类型 | **代码修改** / **项目文档** / **只读分析** |
| 任务标题、明确目标 | 必填；目标要能判定完成 |
| 允许写入路径 | 代码/文档任务必填，每行一个；只读分析不需要 |
| 固定验证命令 | 可选，每行一条；执行后逐条记录通过与否 |
| 任务文档、任务需求 | 从本迭代固定输入里勾选；任务卡会显示需求正文和验收条件 |

点 **「保存任务」**，同一迭代可以加多个。

### 8.2 启动执行

点任务卡的 **「启动 OpenCode」**（重试时显示「新建重试执行」）。Atlas 在**隔离副本**里调用引擎，期间：

- 卡片标签实时刷新：执行状态、结果状态、验收状态。
- **等待工具权限** → 「仅本次允许」/「后续同类允许」/「拒绝」。
- **需要你的输入** → 填答案后「提交答案」或「拒绝回答」。
- 随时 **「停止」**；完成后可以 **「在当前 OpenCode 会话中继续」** 追加指令。
- 有多次尝试时，卡片上出现 **「执行尝试」** 切换钮：旧尝试只读，可以回看状态和证据。

### 8.3 核对并应用结果

执行结束后逐项看证据（**不能只看引擎总结**）：

- **固定验证命令**：绿色 = 全部通过；黄色 = 不完整或未通过，可展开看输出。
- **文件变化**：是否位于声明写入范围内；越界变化会**拒绝应用**。
- **Agent 结构化完成报告**：按需求逐条列状态和证据，但它是引擎提供的信息，**不等于 Atlas 验证或产品验收**。
- 需要时展开 **「查看引擎结果」** 看原文。

确认无误后点 **「应用并更新项目基线」**：

- 应用前会再次核对源工作区和已接受基线；**有冲突不会覆盖现有文件**。
- 成功后写回状态成为**新的已接受基线**。
- 如果出现红条 **「文件已写回，但新项目基线未采用」**：结果不算完整应用，**不能通过验收**；到工作区基线面板检查外部变化并采用当前状态，然后重试。
- 应用中断（状态停在 `applying`）时再次点应用即可：Atlas 会识别已写回的结果直接收尾，未写回的重新执行。

### 8.4 基线前进后重新对齐迭代

应用一个任务后，项目基线会前进，而迭代固定的是旧基线。这时阶段四会显示黄色提示和 **「重新对齐迭代基线」** 按钮：

- 点它把迭代对齐到最新已接受基线，之后才能启动下一项执行；
- 这是**显式**操作，Atlas 不会静默改用最新基线；调整历史可在「基线调整记录」里回看。

### 8.5 产品验收

在任务卡「验收证据或失败原因」里如实填写，然后选择：

- **验收通过**：仅当本次执行是「执行完成」时可选，且证据必填。
- **验收失败**：可补充证据后重新确认。
- **明确免验**：必须填写理由。

失败或停止的执行**不能**验收通过；基线未采用也不能。验收记录会保存在任务卡「验收记录」里，随时可查。

### 8.6 完成或放弃迭代

全部任务验收通过或免验后，点 **「完成迭代」**；也可以 **「放弃迭代」**。结束后迭代进入「历史迭代」，任务、执行尝试和验收记录都**只读**；继续开发要建立新迭代。

### 检查点

- 任务卡结果状态 `applied`、验收状态「通过」，且「应用」步骤不再报错。
- 「当前状态」卡片「下一项」显示「完成迭代」或「当前迭代已完成」。
- 历史迭代里能看到完整的执行尝试与验收记录。

---

## 9. 两条完整路线

### 9.1 新想法（从类型开始）

1. 首页搜索 → 类型页阅读 → **「基于此类型创建项目」**（自动固定类型正文）。
2. 需要时补 1～2 条应用资料 → **「建立项目基线」**。
3. **「分析固定资料」** → 阶段二回答关键问题、确认本版需求、记录决定。
4. 生成核心 4 份 → 逐份审阅 → **「批准当前版本」** → 建立迭代。
5. 加一个范围小的代码任务 → 执行 → 看证据 → 应用 → 验收 → 完成迭代。
6. 需要时在基线面板 **「检查外部变化」**，再建第二轮迭代。

### 9.2 已有项目改进

1. 「项目」页创建项目：填**已存在的本地目录**，类型选「已有项目改进」。
2. 阶段一：**「建立项目基线」**（读结构、清单和重点路径）→ 在「资料分析与文档草案」填**本轮局部改进目标**并 **「生成项目改进方案」**。
3. 阶段二：确认改动范围（当前/期望行为、影响范围、回归条件），补资料。
4. 阶段三：生成并批准文档；建立迭代。
5. 阶段四：任务里写清允许写入路径和固定验证命令；执行、核对、应用、验收。
6. 若在其它工具里改过代码：先 **「检查外部变化」**，受影响的任务重新核对，无关的不必重做。

### 9.3 最佳实践案例

教程里的操作顺序可以复用，但每个项目的资料、范围和验收必须重新确认。案例状态也要看清：

- **已验证：本地优先的个人日计划器。** 从类型叶子开始，首轮只做跨项目当天承诺和本地持久化；第二轮再加入手动时间块与冲突提示。它展示了如何压住范围、保存失败证据，并让第二轮使用第一轮的真实实现状态。完整记录在 `docs/u17-pilots/simple-new-idea/`。
- **已验证并封存：已有项目局部改进。** 先冻结真实代码和回归命令，再规定允许写入路径；本案例已完成普通/Atlas 对照、原子写入与损坏文件错误处理、10 项回归、Atlas 迭代和独立验收。应用前遇到外部变化必须重新核对基线，不能静默覆盖。完整记录在 `docs/u17-pilots/existing-project/`。
- **规划模板：多类型需求交接。** 先明确每个模块的主记录、稳定标识、状态传递和失败恢复，再验证一条最小交接，不要一开始就扩展同步、权限或自动排程。
- **已运行案例：用 Atlas 调研 Agent Harness 并生成开发文档。** 先搜索相关类型和应用，阅读并固定来源，再区分事实、推断、未知和待决策项，最后按依赖生成并批准产品需求、用户流程、技术方案、开发计划和验收计划。短期 OpenCode 集成与长期是否抽象独立 Harness 只作为待验证决策记录；Atlas 的架构评估协议仍是另一条线，不属于这个产品使用案例。运行记录见 `docs/u17-pilots/agent-harness-product/`。
- **规划模板：高风险数据恢复与版本迁移。** 先建立只读基线和备份，再在隔离副本生成迁移预览，确认新增、删除、冲突和回滚路径后才应用；外部变化、失败和验收证据都要保留。
- **规划模板：中断与会话不可用后的继续开发。** 先保存任务、基线、命令和差异证据，导出交接包；新会话核对当前工作区和外部变化后，从下一项动作继续，不能重放全部副作用。

更多案例边界、证据要求和“不要照搬”的部分见 `docs/U17_BEST_PRACTICES.md`。最佳实践是经过证据验证的工作方式，不是可以跳过搜索、确认和验收的固定项目模板。

---

## 10. 导出与交接

项目页右上角 **「导出 Markdown 包」** 会生成自包含目录和 ZIP（含文档版本、需求与决策记录、任务与执行记录），并显示归档路径与 SHA-256。离开 Atlas 界面后，开发者和新的执行会话仍能读懂这个项目的依据与状态。

---

## 11. 草稿：扩充类型库（可选）

「草稿」是知识库自我扩充的入口：从一句话想法生成新的类型叶子，审查后入库。

- 新建叶：填英文名、中文名（可留空由模型起名），「一句话想法」和「调研起点链接」二选一；与现有语料高度相似时需勾选强制创建。
- 审查：看相似度与碰撞候选、点「查重」、编辑正文、生成中文对照。
- 入库：按查重裁决选「归入子域」或「并入现有叶（记别名）」，点「确认归类入库」；正文检查未通过时按钮会被锁住。

这部分不影响项目工作台的使用。

---

## 12. 排障

| 现象 | 原因与处理 |
|------|------------|
| 按钮是灰的 | 多数是前置条件没满足：生成文档需要已确认本版需求；建立迭代需要已接受基线 + 已批准文档 + 已确认需求；应用需要执行完成且文件变化在范围内。看按钮上方的提示或「当前状态」的下一项 |
| 提示缺少资料 / 需求 / 文档批准 / 基线 | 分别去阶段一固定资料、阶段二确认需求、阶段三批准文档、阶段一建立基线 |
| 分析运行失败 | 看失败原因；修正资料后**重新运行**。旧运行保留为历史 |
| 「固定输入已过期」 | 生成后资料/需求/决定有变化。复核差异后点「确认仍要应用过期结果」，或重新生成 |
| 文档变「需要复核」 | 上游需求、文档或基线变了。正文未被改写；复核后保存新版本并重新批准 |
| 提示 rebase / 基线前进 | 到阶段四点「重新对齐迭代基线」，再启动下一项执行 |
| 红条「新项目基线未采用」 | 先到工作区基线面板「检查外部变化」→「采用当前状态为新基线」，然后重试应用或重新验收 |
| 失败/停止的执行不能验收通过 | 这是设计约束。点「新建重试执行」，或填写理由后「明确免验」 |
| 执行「待核对」 | 点「关闭不可用执行」收口；保留的记录仍可查看 |
| 「正式资料已有新版本」 | 项目固定的是收藏时版本，仍然有效；要跟进就回类型页重新收藏 |
| 页面打不开或加载失败 | 服务可能已停止。重新双击 `apps/Atlas.app` 打开 |
| 删了项目，源码还在吗 | 在。「删除」只清理 Atlas 的项目记录，本地工作区和源码不会动 |

---

## 13. 边界与责任

Atlas 刻意不做的事，提前说明：

- **不替你做决定**：AI 的范围建议、完成报告、价值分析都只是输入，确认与验收永远由你完成。
- **不隐藏失败**：失败、停止、未采用、过期都会被明确标出，不能伪装成成功。
- **执行引擎可替换**：Atlas 自己保存项目状态；即使引擎会话丢失，也能从记录恢复并交接下一项任务。
- **首版是本地单用户**：不做账号、协作、云端执行；同一时间维持简单的执行并发。
- **专业结论有边界**：资料只提供到已固定的深度，AI 不编造产品页面、交互或市场结论；不确定的信息会被标为未知或待验证。

---

## 附：常用术语速查

| 中文 | 英文 / 代码 | 一句话 |
|------|-------------|--------|
| 固定资料 | reference | 收藏时固定版本的依据 |
| 候选需求 | requirement | 待你确认范围的需求 |
| 项目决定 | decision | 你记录的取舍与理由 |
| 开放项 | open item | 分析提出的问题/冲突/建议及处理状态 |
| 文档版本 | document version | 不可变正文 + 固定依据 |
| 工作区基线 | workspace baseline | 文件指纹与现状证据 |
| 迭代 | iteration | 冻结的输入 + 任务范围 |
| 执行 | execution | 一次引擎运行及其证据 |
| 应用 | apply | 把隔离副本的结果写回并更新基线 |
| 验收 | acceptance | 你对产品的独立结论 |
